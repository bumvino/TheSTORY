<h2>Events</h2>
<ul>
    <% events.forEach(event => { %>
        <li>
            <strong><%= event.title %></strong><br>
            <%= event.date %> – <%= event.location %><br>
            <em><%= event.description %></em>
        </li>
    <% }) %>
</ul>